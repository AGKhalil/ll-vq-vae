defaults:
- quantizer: vq-ema
- dataset: celeba
- _self_

general:
  entity: entity
  project: ll-vq-vae
  seed: 2
  device: 0

optimizer:
  learning_rate: 1e-3
  scheduler_gamma: 0.0
  weight_decay: 0.0
